# TanqueStudio roadmap — snapshot 2026-07-29

> A point-in-time snapshot, not a living doc. It will drift after a few sessions — that's fine,
> re-ask for a fresh one rather than trying to keep this file current by hand. Sourced from
> `README.md`'s own Roadmap section (synced as of commit `0187aae`) plus Open Brain session notes.

## Where things stand

Current release: **v0.9.32** (2026-07-28, build 25, notarized). Four fixes have landed on `main`
since that tag and are not yet in a released build:

- Story Studio: a new project could render raw noise with no explanation (`defaultConfigJSON`
  pointed at an empty model). Fixed — the controller now refuses a render with no effective
  model and shows why; the default config is Draw Things' Krea 2 Turbo.
- DT Project Browser: Send to Generate / img2img actions used the clip's cover-frame thumbnail
  or a half-size preview instead of the frame being viewed. Fixed — both now resolve through the
  full-size fetch.
- Drawer section headers only toggled from the chevron, not the row. Fixed.
- **Found, not fixed, roadmapped**: Generate only restores 10 of an imported image's 58 config
  fields (LoRAs, hires fix, tiling, refiner, negative prompt, SDXL conditioning all get parsed
  and discarded). Plan is to build a raw-JSON metadata viewer first — the diagnostic that would
  have caught this — then widen the applier.

Repo: `main` at `0187aae`, everything pushed, test suite 168, zero failures.

## Immediate next step

- [x] ~~Cut a release (v0.9.33)~~ — **DONE 2026-07-29.**
  https://github.com/skeptict/TanqueStudio/releases/tag/v0.9.33, build 26, notarized, stapled,
  confirmed Latest. Full checklist run in order; 161 passed / 0 failed before the bump.

**Three commits have landed since that tag and are not in any released build** — they ride in
0.9.34:

- [x] **Aspect-ratio chips land on their ratio** (`ba06e82`) — the 64px grid rounding is now a
  four-corner search, and a chip reads as active when it is the canvas that chip produces
  rather than against a fixed 0.02 tolerance.
- [x] **Size tiers match Draw Things** (`0f579da`) — 768² / 1024² / 1280², plus our own XL at
  1536². Feeding DT's budgets through the corner search reproduces DT's table exactly.
- [x] **`.mp4` export carries the clip's soundtrack** (`3e3495a`) — AAC 192k via AVFoundation,
  plus BT.709 colour tags and the DT config as a metadata comment.

Next release should be **v0.9.34** carrying those three.

## Remaining issues, in README priority order

1. **Export All should offer `.mp4`** — ⬅ **IN PROGRESS, and now unblocked.** It still pages
   over raw database rows rather than the grouped slots the grid shows, so a 5-clip project
   writes ~1,285 loose frames. ~~Decide the audio question first~~ — **settled 2026-07-29**:
   `VideoAssembler` muxes the clip's soundtrack as AAC, so a bulk `.mp4` export will carry
   sound. What remains is walking slots instead of rows, plus a format chooser.
2. **Video handling — general pass** — playback, audio, and frame-accurate Send to Generate /
   Export This Frame have all shipped. Still open: a wider look at how series are handled outside
   the DT Project Browser, and whether "expandable in place" (spec §7) is still wanted now that
   clip playback and the detail scrubber exist.
3. **Story Studio configs — make the base config easy to change** — it's currently a Swift
   string literal requiring a rebuild to tune. Four-step plan already scoped: "Use a saved
   config…" picker in Project Info → make the new-project default a setting → reuse the same
   mechanism everywhere a config already exists → "send render to new Story Studio project."
   **Decided:** snapshot, not live reference — a live link would retroactively change what
   existing projects render, which collides with the reproducibility work already shipped.
4. **Generate drops most of an imported image's settings** — see above. Build the raw-JSON
   viewer first.
5. **Render queue — matrix in, job list out** — replaces the dead "Workflow Builder" Labs stub.
   Design is fully settled: define axes → Expand → flat queue of full standalone-config jobs →
   prune/reorder/pause/run. One engine pass per job (failure isolation). Compiles to StoryFlow,
   following the Story Studio precedent — not built inside the StoryFlow editor or inside
   Generate. Just needs building.
6. **README polish** — screenshots, demo GIF.

## Backlog / longer-term goals

- StoryFlow Phase 4 — LLM-backed `enhance`/`interrogate`, routed through Tanque Studio's own LLM
  stack rather than Draw Things' answer model.
- StoryFlow Phase 5 — Vision-framework canvas ops (faceZoom, background removal, masks, depth /
  pose extraction). No gRPC path exists for any of these — its own project.
- StoryFlow polish — replace-mode toggle, image-variable drag-drop import, end-to-end UX pass.
- Patterns Studio integration — WKWebView panel or PNG export feeding img2img.
- Gallery collections / organization.
- Soft-edged inpaint brush (mask transport is binary today).
- Intel Mac launch failure — root cause unknown, low priority.

## Open questions — nobody's call but yours

- [x] ~~Should `.mp4` export carry audio, or should the UI just say plainly that it doesn't?~~
  **ANSWERED 2026-07-29: it carries audio.** dtm proved it was worth doing, and it turned out
  every piece but the mux already existed here. Verified on a real clip at −28.7 dBFS RMS.
- Is "expandable in place" for series still worth building now that playback exists?
- What did "series outside the DT Project Browser" originally mean? Flagged for your own review,
  never resolved.

## New idea logged this session

- **img2img from a blank/abstract canvas to generate faces or other subjects** — sparked by
  seeing someone paint broad color blobs and run img2img on it. TanqueStudio's Color Draw mode
  already supports painting on a blank canvas and flattening to img2img source, so this is
  mostly there — see the canvas-size-on-blank-canvas issue below, found while testing this.
  - [x] ~~canvas-size-on-blank-canvas issue~~ — **investigated 2026-07-29 and it did not
    exist.** `ColorDrawLayer`'s config, canvas size and rendered rect agree to three decimals
    on every path. The real defect was the aspect-ratio chip rounding, now fixed (`ba06e82`).
    The drawer-sections bug reported alongside it was also not real — that build predated
    `a988727` by eight hours. Both write-ups are in
    `Docs/colordraw-blank-canvas-size-kickoff-prompt.md`.
  - **Still open from that doc, and unaffected**: Feature 3 (a genuinely blank Color Draw
    canvas via `colorDrawBaseImage` + a New Canvas button) and Feature 4 (fill bucket).
