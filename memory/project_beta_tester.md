---
name: Beta tester contributor
description: A beta tester has forked the repo and will open a PR; review expected
type: project
---

A beta tester has forked DrawThingsStudio and will submit a PR when ready (noted Mar 12 2026).

**Why:** Community testing/contribution via GitHub PR workflow.

**How to apply:** When reviewing their PR, check against project conventions: neumorphic design system (`NeumorphicStyle.swift`), `@MainActor final class` ViewModels, `sheet(item:)` pattern for conditional sheets, `PBXFileSystemSynchronizedRootGroup` (new Swift files auto-included), SwiftData models registered in `DrawThingsStudioApp.swift`.
